#Without __init__.py, Python may not recognize the directory as a package, leading to import errors. It’s a best practice to include it (even empty) for clarity and functionality.
#from constants import *
# now this has imported everything in from checkers file and hence this will return send every constant back to the main file when called